#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    if (argc != 2)
    {
        printf("Wrong input length!\n");
        return 1;
    }
    bool f = only_digits(argv[1]);
    if (num == true)
    {
    printf("%s\n", argv[1]);
    }
}